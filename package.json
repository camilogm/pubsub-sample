{
  "name": "pubsub",
  "version": "1.0.0",
  "description": "Install json-server with npm i -g json-server",
  "main": "create-config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/pubsub": "^2.18.4",
    "axios": "^0.24.0"
  }
}
